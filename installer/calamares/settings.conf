# NEURONIX Calamares Installer Configuration
modules-search: [ local ]
branding-search: [ local, /etc/calamares/branding ]

instances:
  - id:       neuronix-engine
    module:   shellprocess
    config:   shellprocess-neuronix.conf

sequence:
  - show:
      - welcome
      - locale
      - keyboard
      - partition
      - users
      - summary
  - exec:
      - partition
      - mount
      - shellprocess@neuronix-engine
      - users
      - umount
  - show:
      - finished

branding: neuronix
prompt-install: true
dont-chroot: false
oem-setup: false
disable-cancel: false
disable-cancel-during-exec: false
hide-back-and-next-during-exec: true
quit-at-end: false
